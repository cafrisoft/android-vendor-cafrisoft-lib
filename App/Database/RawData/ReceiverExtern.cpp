#include "Receiver.hpp"

namespace TESysDBRawData {
        
#if 0
    bool Receiver::BoardCastToSaver(std::shared_ptr<TESys::Net::PacketDataAcq> notifyPack) {
    
        return this->GetSaver()->BroadCast(notifyPack);
    }
#endif

};
